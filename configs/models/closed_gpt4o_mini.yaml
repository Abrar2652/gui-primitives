key: closed_gpt4o_mini
family: closed_api
hf_id: gpt-4o-mini                # closed reference; run only on the human-verified core
dtype: null
device_map: null
coord_space: pixel
supports_attention_capture: false
api:
  provider: openai
  env_key: OPENAI_API_KEY
  max_items: 2000                  # hard cap to stay inside the $50 budget
